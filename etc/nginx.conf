server {
	listen [::]:80;
	listen 80;
	server_name localhost;

	location ^~ /uploads/ {
		root /home/box/web;
	}

	location ~ \.[a-zA-Z0-9]+$ {
		root /home/box/web/public;
	}

	location /hello/ {
		include proxy_params;
		proxy_pass http://127.0.0.1:8080;
	}

	location / {
		return 404;
	}
}
